div class="row"
  div class="col-xs-4"
    h4
      i class="icon-tags"
      = t('.title')
  div class="col-xs-8 text-right"
    h4 = link_to t('.add'), new_link_list_path, class: 'button-admin-menu'

div class="row"
  - if @link_lists.blank? && !params[:page].present?
    = render partial: 'blank'
  - else
    = render partial: 'list'