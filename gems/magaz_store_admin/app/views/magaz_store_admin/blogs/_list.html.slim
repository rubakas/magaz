= paginate @blogs

table class="table table-striped"
  thead
    tr
      th = t('.title')
      th

  tbody
    - @blogs.each do |blog|
      tr
        td = link_to blog.title, [ blog]
        td = link_to t('.delete'), [ blog], method: :delete, data: { confirm: t('.confirmation') }

br

= paginate @blogs