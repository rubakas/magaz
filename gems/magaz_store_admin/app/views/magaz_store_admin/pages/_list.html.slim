= paginate @pages

table class="table table-striped"
  thead
    tr
      th = t('.title')
      th

  tbody
    - @pages.each do |page|
      tr
        td = link_to page.title, [ page]
        td = link_to t('.delete'), [ page], method: :delete, data: { confirm: t('.confirmation') }

br

= paginate @pages